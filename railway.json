{
  "build": {
    "builder": "NIXPACKS"
  },
  "start": "sh -lc 'rm -f /app/.env /app/src/.env 2>/dev/null || true; export ENVIRONMENT=production; exec uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
}
